// merge.js
const fs = require("fs");
const path = require("path");

console.log("🟢 merge.js starting…");

// List of league playlists to merge
const playlists = [
  "nba.m3u",
  "nhl.m3u",
  "mlb.m3u",
  "nfl.m3u",
  "mls.m3u",
  "wnba.m3u",
  "ufc.m3u",
  "boxing.m3u"
];

let merged = "#EXTM3U\n";
let addedCount = 0;

playlists.forEach(file => {
  const filePath = path.join(__dirname, file);

  if (fs.existsSync(filePath)) {
    console.log(`➡️ Adding ${file}`);
    const content = fs.readFileSync(filePath, "utf8");

    // Remove any extra #EXTM3U headers
    merged += content.replace(/^#EXTM3U\s*/i, "") + "\n";
    addedCount++;
  } else {
    console.warn(`⚠️  Missing file: ${file} (skipping)`);
  }
});

// Trim extra blank lines
merged = merged.trim();

// Write merged playlist
const outputFile = path.join(__dirname, "merged.m3u");
try {
  fs.writeFileSync(outputFile, merged, "utf8");
  console.log(`✅ Merged playlist saved as ${outputFile}`);
  console.log(`ℹ️ Total playlists merged: ${addedCount} / ${playlists.length}`);
} catch (err) {
  console.error(`❌ Failed to write merged playlist: ${err}`);
}

// Always exit with code 0 to avoid workflow failure
process.exit(0);
